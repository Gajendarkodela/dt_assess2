<% if flash[:notice].present? %>
    <div class="alert alert-success" role="alert">
      <%= flash[:notice] %>
    </div>
<% end %>

<button onclick="goBack()" class="btn btn-primary">Back</button>

<%=render 'form_test_case'%>
